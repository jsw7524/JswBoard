@model IEnumerable<MyCard3.Models.Board>

@{
    ViewBag.Title = "Index";
}

<h2>看版清單</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.Name)*@
            看版名稱:
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @*@Html.DisplayFor(modelItem => iteHtml.DisplayNameForm.Name)*@
            @Html.ActionLink(item.Name, "Index", "Articles", new { boardId = item.Id },null)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}
</table>
